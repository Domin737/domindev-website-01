// Tutaj będą funkcje pomocnicze
export const formatDate = (date) => {
  return new Date(date).toLocaleDateString("pl-PL");
};

// Możesz dodać więcej funkcji pomocniczych w przyszłości
// export const anotherHelper = () => { ... };
